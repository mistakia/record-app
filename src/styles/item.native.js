import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    marginBottom: 15,
    padding: 10
  },
  label: {
    fontSize: 10,
    padding: 10,
    paddingBottom: 0
  }
})

export default styles
