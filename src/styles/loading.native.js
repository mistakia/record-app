import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  button: {
    marginTop: 30,
    marginBottom: 20,
    marginLeft: 15,
    marginRight: 15
  }
})

export default styles
